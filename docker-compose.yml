services:
  homelab-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_HOME_LAB_IP=10.0.0.134
    ports:
      - "8080:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
